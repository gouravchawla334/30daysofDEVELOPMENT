
/*
to compare two variable we use " == "
to assign the value we use " = " or " !="
to check both value and type we use " === " or " !=="
*/

let mark = 0;

if (mark == 0) {
    console.log("Marks is Zero");  
} else {
    console.log('marks is not zero');
}
